---
description: Install Python dependencies from requirements.txt
allowed-tools: Bash
---

Run `pip install -r requirements.txt` and report:
- Whether installation succeeded or failed
- Any packages that failed to install and why
